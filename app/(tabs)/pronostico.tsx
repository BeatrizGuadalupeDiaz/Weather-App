import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const pronosticoScreen = () => {
  return (
    <View>
      <Text>pronostico</Text>
    </View>
  )
}

export default pronosticoScreen

const styles = StyleSheet.create({})